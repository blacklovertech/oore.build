# Feature Docs

Every user-facing feature must add or update at least one markdown file in this directory.

Use the template at `docs/templates/feature-doc-template.md`.

Naming convention:

- `YYYY-MM-DD-short-feature-name.md`

Required sections are enforced by CI.
